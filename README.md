# lora-mnist-fashionmnist
Using Low-Rank Adaptation (LoRA) to efficiently fine-tune a neural network from the MNIST dataset to the Fashion-MNIST dataset using a simple fully connected neural network.
